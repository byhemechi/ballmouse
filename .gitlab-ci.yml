image: python:alpine

before_script:
  - pip install mkdocs
  - pip install mkdocs-material

pages:
  script:
  - mkdocs build
  - mv site public
  - mv index.html public
  - mv dist public
  artifacts:
    paths:
    - public
  only:
  - master
