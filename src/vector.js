class Vector {
    /**
     *  A 2D Vector class. Doesn't do much at all, pretty much just a helper.
     * @param {number} x 
     * @param {number} y 
     */
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}